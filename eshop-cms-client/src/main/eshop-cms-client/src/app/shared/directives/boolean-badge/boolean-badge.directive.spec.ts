import { BooleanBadgeDirective } from './boolean-badge.directive';

describe('BooleanBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new BooleanBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
